describe('devNoticeService', function() {

  beforeEach(module('common'));

  it('should ...', inject(function(devNoticeService) {

	//expect(devNoticeService.doSomething()).toEqual('something');

  }));

});